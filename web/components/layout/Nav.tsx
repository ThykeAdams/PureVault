import React, { FC } from 'react';

interface BaseProps {
}

const NavBar: FC<BaseProps> = ({}) => {
  return <div className="w-screen h-14 bg-background-secondary">
</div>
};

export default NavBar;
